# 22 — Showcase — Vantrilex Next-Gen Agentic Workbench & Launcher

## Purpose

DESIGN.md-governed project showcase pages generated per workspace.

## Status

The `project-showcase-builder` skill owns generation.

## Schema

### Inputs

`VISION.md`, `PROJECT-CONTEXT.md`, architecture docs, live test metrics
from the 11-TESTING.md commands, component maps.

### Output

Zero-dependency `docs/showcase.html`: serif hero, feature-card grid,
dark code-window card with real snippets, metrics cards, interactive
knowledge graph on Canvas with a WebGL upgrade path. Inline CSS and JS;
no external fonts, no CDN, no build step. Footer carries a subtle
Generated by Vantrilex badge.
